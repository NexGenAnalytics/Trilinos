

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_Tpetra)
ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_TpetraExt)
# ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_ML)
IF (${PACKAGE_NAME}_ENABLE_Tpetra AND ${PACKAGE_NAME}_ENABLE_TpetraExt) # AND ${PACKAGE_NAME}_ENABLE_ML
  TRIBITS_ADD_EXECUTABLE(
    PCPG_Tpetra_File_Ex
    SOURCES PCPGTpetraExFile.cpp
    COMM serial mpi
    # ARGS --verbose
    )
ENDIF()
